import { ArrowCounterClockwise, BracketsCurly, FileCsv, Printer } from "@phosphor-icons/react";
import { exportCsv, exportJson } from "../lib/export";

export function ExportCenter({ categories, lots, inventoryItems, resetData }) {
  return <div className="view-stack"><section className="page-heading"><div><p className="eyebrow">Export center</p><h1>Take the inventory with you</h1><p>Download buyer menus, internal records, and structured backups from this device.</p></div></section><section className="export-grid"><button className="export-card" onClick={() => exportCsv(categories, lots)}><span><FileCsv size={30} weight="duotone" /></span><div><h2>Lot register CSV</h2><p>Every built lot with category, quantity, price, status, and storage location.</p><strong>Download .csv</strong></div></button><button className="export-card" onClick={() => exportJson(categories, lots, inventoryItems)}><span><BracketsCurly size={30} weight="duotone" /></span><div><h2>Full inventory backup</h2><p>Categories, measurements, dynamic variables, object items, lots, rules, and media progress.</p><strong>Download .json</strong></div></button><button className="export-card" onClick={() => window.print()}><span><Printer size={30} weight="duotone" /></span><div><h2>Printable inventory report</h2><p>Print the current view or save it as a PDF from the browser print dialog.</p><strong>Open print dialog</strong></div></button></section><section className="panel reset-panel"><div><p className="eyebrow">Demo controls</p><h2>Restore the original seed inventory</h2><p>This replaces local changes on this device with the example categories, object items, and lots.</p></div><button className="danger-button" onClick={() => { if (window.confirm("Replace local changes with the original seed data?")) resetData(); }}><ArrowCounterClockwise /> Reset demo data</button></section></div>;
}
